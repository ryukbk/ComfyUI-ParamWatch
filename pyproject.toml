[project]
name = "comfyui-paramwatch"
description = "Collect a named parameter's value from any node in the workflow: watch widget names (e.g. 'ckpt_name, unet_name'), pick a collected node from a dropdown, and output its value."
version = "0.1.0"
license = "AGPL-3.0-or-later"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[project.urls]
Repository = "https://github.com/<you>/ComfyUI-ParamWatch"

# ComfyUI Registry metadata — https://docs.comfy.org/registry/specifications
[tool.comfy]
PublisherId = "<your-publisher-id>"
DisplayName = "ComfyUI-ParamWatch"
Icon = ""
Tags = ["utility", "workflow", "parameter", "inspect"]
